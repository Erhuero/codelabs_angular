import { GestionErreurPipe } from './gestion-erreur.pipe';

describe('GestionErreurPipe', () => {
  it('create an instance', () => {
    const pipe = new GestionErreurPipe();
    expect(pipe).toBeTruthy();
  });
});
